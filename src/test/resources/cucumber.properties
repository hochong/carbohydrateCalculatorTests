# cucumber.properties
# Cucumber global configuration \u2014 picked up automatically from the classpath root.
# Maven places src/test/resources on the test classpath, so no extra setup is needed.
 
# Suppress the "share your Cucumber report" banner in the console
cucumber.publish.quiet=true
 
# Disable automatic report publishing to cucumber.io
cucumber.publish.enabled=false