base.url=https://www.calculator.net/carbohydrate-calculator.html
implicit.wait.seconds=10
explicit.wait.seconds=15
page.load.timeout.seconds=30
# Set to true to run Chrome without a visible window (required for CI)
# Override at runtime: mvn test -Dheadless=true
headless=false
